import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thelittlemillers.tlm',
  appName: 'tlm',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
